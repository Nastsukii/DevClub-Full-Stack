const name = 'luiz'
const user = ['matheus', 'gabriel', 'junior']
// pega item por item e mostra na tela
for (const letter of name) { // ele le cada letra da variavel name
    console.log(letter)
}

for (const nome of user) { // vai mostrar item por item
    console.log(nome)
}